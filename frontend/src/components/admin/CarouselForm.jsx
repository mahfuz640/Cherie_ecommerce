export default function CarouselForm({ editing, image, onImageChange, onUpload, onSave, onCancel, slides, onEdit, onDelete, apiImg }) {
  return <section className="admin-form"><h2>{editing ? 'Edit carousel image' : 'Add carousel image'}</h2><form key={editing?._id || 'new-slide'} onSubmit={onSave}><input name="title" defaultValue={editing?.title} placeholder="Slide title" /><input name="subtitle" defaultValue={editing?.subtitle} placeholder="Slide subtitle" /><input name="link" defaultValue={editing?.link || '#collection'} placeholder="Button link" /><input name="image" required value={image} onChange={event => onImageChange(event.target.value)} placeholder="Carousel image URL" /><label className="upload">Upload carousel image<input type="file" accept="image/*" onChange={event => onUpload(event.target.files[0])} /></label><div className="form-actions"><button className="button">{editing ? 'Update carousel' : 'Add to carousel'}</button>{editing && <button type="button" onClick={onCancel}>Cancel</button>}</div></form><div className="slide-list">{slides.length ? slides.map(slide => <div className="slide-admin" key={slide._id}><img src={apiImg(slide.image)} alt={slide.title || 'Carousel image'} /><span>{slide.title || 'Untitled slide'}</span><button type="button" onClick={() => onEdit(slide)}>Edit</button><button type="button" className="danger" onClick={() => onDelete(slide)}>Remove</button></div>) : <p>No carousel image yet.</p>}</div></section>;
}
