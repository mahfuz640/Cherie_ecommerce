export default function ProductForm({ editing, image, onImageChange, onUpload, onSave, onCancel }) {
  return <section className="admin-form"><h2>{editing ? 'Edit product' : 'Add a new product'}</h2><form key={editing?._id || 'new-product'} onSubmit={onSave}><input name="name" required defaultValue={editing?.name} placeholder="Product name" /><input name="price" required type="number" min="0" defaultValue={editing?.price} placeholder="Price" /><input name="category" defaultValue={editing?.category || 'Jewellery'} placeholder="Category" /><input name="stock" type="number" min="0" defaultValue={editing?.stock ?? 0} placeholder="Stock" /><textarea name="description" required defaultValue={editing?.description} placeholder="Product description" /><input name="image" value={image} onChange={event => onImageChange(event.target.value)} placeholder="Image URL (or upload below)" /><label className="upload">Upload product image<input type="file" accept="image/*" onChange={event => onUpload(event.target.files[0])} /></label><label className="checkbox"><input name="featured" type="checkbox" defaultChecked={editing?.featured} /> Featured product</label><div className="form-actions"><button className="button">{editing ? 'Update product' : 'Publish product'}</button>{editing && <button type="button" onClick={onCancel}>Cancel</button>}</div></form></section>;
}
